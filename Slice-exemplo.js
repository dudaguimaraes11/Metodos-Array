// índice inicial (inclusivo), índice final (exclusivo)
let playlist = ["Funk", "Hipp-Hop", "Pop", "Rock", "Jazz", "Reggae", "Classical", "Indie"];

console.log (`Parte da playlist (índices 2 a 5): ${playlist.slice(2,6)}).`);
console.log (`Parte da playlist (índices 0 a 3): ${playlist.slice(0,4)}).`);
console.log (`Parte da Playlist (índice 4): ${playlist.slice(3,4)}.`);
